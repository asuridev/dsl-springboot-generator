system:
  name: test-query-scalar
  description: Test system for canonical scalar types as query returns.
  domainType: core

boundedContexts:
  - name: item
    type: core
    purpose: Manages inventory items.
    aggregates:
      - name: Item
        root: Item
        entities: []

infrastructure:
  database:
    type: relational
    isolationStrategy: schema-per-bc
