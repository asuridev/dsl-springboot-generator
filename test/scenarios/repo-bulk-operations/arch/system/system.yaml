system:
  name: test-repo-bulk
  description: Test system for bulkOperations on a repository (D3 — List<UUID> param UUID import).
  domainType: core

boundedContexts:
  - name: inventory
    type: core
    purpose: Manages inventory items.
    aggregates:
      - name: Item
        root: Item
        entities: []

infrastructure:
  database:
    type: relational
    isolationStrategy: schema-per-bc
