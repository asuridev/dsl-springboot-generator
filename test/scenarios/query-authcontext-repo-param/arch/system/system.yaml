system:
  name: test-query-authcontext-repo-param
  description: Test system for query repository params sourced from authContext.
  domainType: core

boundedContexts:
  - name: catalog
    type: core
    purpose: Manages products.
    aggregates:
      - name: Product
        root: Product
        entities: []

infrastructure:
  database:
    type: relational
    isolationStrategy: schema-per-bc
