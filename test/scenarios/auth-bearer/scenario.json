{
  "description": "auth.type: bearer on externalSystem. Verifies @Value bearer token interceptor, no dead RequestInterceptor import, and secret placeholder in urls.yaml.",
  "expectFailure": false
}
