system:
  name: test-name-invalid-identifier-aggregate-system
  description: Test system for aggregate-name identifier validation.
  domainType: core

boundedContexts:
  - name: catalog
    type: core
    purpose: Manages products.
    aggregates:
      - name: Order-Item
        root: Order-Item
        entities: []

infrastructure:
  database:
    type: relational
    isolationStrategy: schema-per-bc
