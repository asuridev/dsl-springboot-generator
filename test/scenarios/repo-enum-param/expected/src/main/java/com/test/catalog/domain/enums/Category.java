package com.test.catalog.domain.enums;

/**
 * Category
 * Product category — non-conventional name (does NOT end in Status/Type/State).
 */
public enum Category {
    ELECTRONICS,
    CLOTHING,
    FOOD
}
