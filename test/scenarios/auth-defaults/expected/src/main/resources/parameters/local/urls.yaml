integration:

  catalog.base-url: https://api.catalog.example.com


  # ── auth secrets — configure via environment variables ──────────────────

  # integration.catalog.bearer-token: ${CATALOG_BEARER_TOKEN}


