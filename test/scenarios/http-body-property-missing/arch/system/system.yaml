system:
  name: test-http-body-property-missing-system
  description: Test system for missing OpenAPI body property validation.
  domainType: core

boundedContexts:
  - name: catalog
    type: core
    purpose: Manages products.
    aggregates:
      - name: Product
        root: Product
        entities: []

infrastructure:
  database:
    type: relational
    isolationStrategy: schema-per-bc
