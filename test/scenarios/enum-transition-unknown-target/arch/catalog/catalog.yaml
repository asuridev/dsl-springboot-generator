bc: catalog
version: "1.0.0"

enums:
  - name: ProductStatus
    description: Lifecycle states of a Product.
    values:
      - value: DRAFT
        transitions:
          - to: NONEXISTENT
            triggeredBy: UC-PRD-001 ActivateProduct
      - value: ACTIVE
        transitions: []
