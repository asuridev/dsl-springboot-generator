system:
  name: test-vo-domain-refs-system
  description: Test system for domain-reference VO property generation scenarios.
  domainType: core

boundedContexts:
  - name: catalog
    type: core
    purpose: Exercises enum and nested-VO references as VO properties.
    aggregates:
      - name: Item
        root: Item
        entities: []

infrastructure:
  database:
    type: relational
    isolationStrategy: schema-per-bc
