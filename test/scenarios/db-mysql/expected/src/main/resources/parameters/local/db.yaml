spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test_event_system
    username: postgres
    password: postgres
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

logging:
  level:
    root: INFO
    com.test: DEBUG
