integration:

  payment-gateway.base-url: ${PAYMENT_GATEWAY_URL:https://api.payment-gateway.example.com}


  # ── auth secrets — configure via environment variables ──────────────────

  # integration.payment-gateway.api-key: ${PAYMENT_GATEWAY_API_KEY}


