bc: catalog
version: "1.0.0"

enums:
  - name: ProductStatus
    description: Lifecycle states of a Product.
    values:
      - value: DRAFT
      - description: This entry has a description but no value or name field.
        # 'value:' and 'name:' intentionally omitted — must fail validation
