system:
  name: test-query-optional
  description: Test system for Optional return types on queries and commands.
  domainType: core

boundedContexts:
  - name: item
    type: core
    purpose: Manages inventory items.
    aggregates:
      - name: Item
        root: Item
        entities: []

infrastructure:
  database:
    type: relational
    isolationStrategy: schema-per-bc
