system:
  name: test-http-schema-ref-missing
  description: Test system for unresolved OpenAPI schema refs.
  domainType: core

boundedContexts:
  - name: catalog
    type: core
    purpose: Manages products.
    aggregates:
      - name: Product
        root: Product
        entities: []

infrastructure:
  database:
    type: relational
    isolationStrategy: schema-per-bc
