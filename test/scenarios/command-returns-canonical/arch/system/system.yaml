system:
  name: test-canonical-returns
  description: Test system for canonical primitive types as command returns.
  domainType: core

boundedContexts:
  - name: item
    type: core
    purpose: Manages inventory items.
    aggregates:
      - name: Item
        root: Item
        entities: []

infrastructure:
  database:
    type: relational
    isolationStrategy: schema-per-bc
