{
  "description": "Value Object generation: covers all validation guards — notEmpty, minLength, numeric min/max on Integer and Long (including large Long values requiring L suffix), positive/negative shortcuts, Decimal min/max, and multiple Decimal fields with multiple eqDecimal() calls in equals()."
}
