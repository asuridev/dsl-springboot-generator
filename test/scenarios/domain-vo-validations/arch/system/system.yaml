system:
  name: test-vo-validations-system
  description: Test system for VO validation-guard generation scenarios.
  domainType: core

boundedContexts:
  - name: catalog
    type: core
    purpose: Exercises all validation guard combinations in Value Objects.
    aggregates:
      - name: Item
        root: Item
        entities: []

infrastructure:
  database:
    type: relational
    isolationStrategy: schema-per-bc
