# Files generated by accidentally running `dsl-springboot build` directly in a scenario dir
src/
build/
.gradle/
settings.gradle
build.gradle
gradlew
gradlew.bat

# Keep test fixtures and golden files
!assertions.json
!dsl-springboot.json
!scenario.json
!arch/
!expected/
