package <%= packageName %>;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.cloud.openfeign.EnableFeignClients;
import org.springframework.data.jpa.repository.config.EnableJpaAuditing;
<% if (outboxEnabled) { %>import org.springframework.scheduling.annotation.EnableScheduling;
<% } %>
/**
 * Main entry point for <%= systemName %>.
 * Generated by dsl-springboot-generator.
 */
@SpringBootApplication
@EnableJpaAuditing
@EnableFeignClients
<% if (outboxEnabled) { %>@EnableScheduling
<% } %>public class <%= applicationClassName %> {

    public static void main(String[] args) {
        SpringApplication.run(<%= applicationClassName %>.class, args);
    }
}
