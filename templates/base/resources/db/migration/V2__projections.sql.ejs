-- derived_from: bc.<bc>.projections[*] (persistent: true)
-- Persistent local read models materialized from upstream events.
-- Auto-generated by dsl-springboot-generator.

<% projections.forEach(p => { %>
-- ── <%- p.bcName %>.<%- p.projectionName %> ───────────────────────────────────────
-- source: <%- p.sourceFromBc %>.<%- p.sourceEvent %>  upsert: <%- p.upsertStrategy %>
CREATE TABLE IF NOT EXISTS <%- p.tableName %> (
    <%- p.keyColumnName %> <%- p.keySqlType %> NOT NULL PRIMARY KEY<% if (p.columns.length > 0 || true) { %>,<% } %>
<% p.columns.forEach((c, i) => { %>    <%- c.name %> <%- c.sqlType %><%- c.notNull ? ' NOT NULL' : '' %>,
<% }); %>    last_updated_at TIMESTAMP NOT NULL
);

<% }); %>
