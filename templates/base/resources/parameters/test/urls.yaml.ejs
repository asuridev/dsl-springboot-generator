integration:
<% httpIntegrations.forEach(function(item) { %>
  <%= item.toBc %>.base-url: ${<%= item.envVar %>:<%= item.localUrl %>}
<% }); %>
<% if (authSecrets && authSecrets.length > 0) { %>
  # ── auth secrets — configure via environment variables ──────────────────
<% authSecrets.forEach(function(s) { %>
  # <%= s.propertyKey %>: ${<%= s.envVar %>}
<% }); %>
<% } %>
